{% set name = "CADET-Python" %}
{% set version = "1.0.4" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/cadet/{{ name }}/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 7fbb021f3a985143728f8fdefae009f6b6a1c9e652d04a2fdc215ad3c796cd19

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - setuptools
    - python {{ python_min }}
    - pip
  run:
    - python >={{ python_min }}
    - addict
    - numpy
    - h5py
    - filelock
    - cadet >=5.0.2

test:
  imports:
    - cadet
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/cadet/CADET-Python
  summary: File based Python Interface for CADET
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - schmoelder
    - ronald-jaepel
